describe Ast do
  # This is the main class. Really a wrapper for the main action.
  it 'is structed as an AST' # More specific
  it 'can be reduced' # `Ast.new('\x.x').reduce!
  it 'can be represented as a string'
end
